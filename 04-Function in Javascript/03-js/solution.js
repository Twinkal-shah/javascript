(function() {
    // Define the number for which we want to calculate the square
    let number = 5;
    
    // Calculate the square of the number
    let square = number * number;
    
    // Display the result
    console.log(`The square of ${number} is ${square}.`);
})();