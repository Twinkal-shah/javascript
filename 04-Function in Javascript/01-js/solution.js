// Arrow function to calculate square of a number
const square = (number) => {
    return number * number;
};

// Example usage
let num = 5; // Number to calculate square
let result = square(num); // Calculate square using the arrow function

// Display the result
console.log(`The square of ${num} is ${result}.`);